# D31 unified fix — build record (C0 freeze)

**Date:** 2026-06-15  **Status:** C0 committed before any code.
Seen-only re-validation; matrix NOT fired; no push.

## Unified root cause (from the remaining-category seen check)

`V2Conductor.compile_and_arm` (`conductor/run_v2_loop.py:68`) calls
`compile_pipeline(soft, world_rev=...)` — **under-parameterized** vs the standalone
pipeline. It drops three optional arguments the matrix needs:

| dropped arg | consequence |
|---|---|
| `world` | §4 gate probe never arms → gate assumption → UNCOVERED→caution → **TOOL_CONTRACT MISS** |
| `auth_token` | docs/authed gate shadow unreadable |
| `planned_write_set` | worker's legitimate write reads as drift → **CLEAN FALSE POSITIVE** (b1+clean, FIR 1.0) |

Plus two unbuilt/unwired pieces:
- the guaranteed **pre-completion sweep** (D28/D29 §3.1) is declared-but-unbuilt
  (`corroboration.py:175`) → a never-touched load-bearing surface (the gate shadow) is never
  re-observed;
- **GATE_SHADOW** probes route to the generic empty-`Invariant()` evaluator in
  `arms.py:_signal_for` instead of `evaluate_gate_probe` → CLEAN even if armed.

## C0 corpus check (category-blind, plan declarations only)

`analysis/d31_corpus_check.py` → `runs/d31_corpus_check.json`:

```
a1/c1/d1: no content writes (only auth/gate/packaging endpoints)
b1: PUT config/settings.yaml (w1), PUT src/*.py (w2)
Q1 any surface written > once?         NO
Q2 any surface written-by-A read-by-B? NO
POLICY: SINGLE-EPOCH (item-5 conditionals -> named residuals)
```

So D31's write-surface policy stays single-epoch; the ordered write-epoch sequence and the
LIGHT cross-worker rule are named residuals (no seen cell exercises them).

## Fix plan (checkpoints; each committed, tests both flag states)

- **C0** custody + corpus check + D31 deviation (this commit, before code).
- **C1** parameterization: `compile_and_arm` passes `world` (side-channel handle), `auth_token`,
  `planned_write_set` to `compile_pipeline` incl. replans; the §4 non-perturbation check made
  counter-neutral (vectors via `/admin` reads, canary POST removed; `/admin/state` exposes
  `validated_docs`). Assert gate probe arms; planned_write_set reaches compiler; injection
  call-index unchanged with/without the gate-probe compile.
- **C2** build the guaranteed pre-completion sweep (D29): re-observe every load-bearing surface
  not seen recently before run end, incl. never-touched shadow surfaces; side-channel, $0,
  within cap, uncovered valve.
- **C3** route GATE_SHADOW / HARD_INVARIANT probes through `evaluate_gate_probe` (`enforcing ==
  True`).
- **C4** write-surface footprint policy: off-footprint monitored vs arm-time baseline;
  in-footprint verify-or-UNCOVERED_CAUTION; persistence-after-epoch on write surfaces.
- **C5** re-validate the full seen sweep (all five categories) through the real V2Conductor for
  the right reason; every clean cell QUIET incl. b1+clean.

## Build outcome (per checkpoint; commits f35c75f → 488eb44)

- **C1** (8aecd7e) parameterize the compile. `compile_and_arm` passes `world` (a
  perturbation-isolated side-channel handle), `auth_token`, `planned_write_set`. The §4
  non-perturbation trapdoor made counter-neutral (all three vectors via the
  middleware-excluded `/admin/state`, which now exposes `validated_docs`; the counting
  canary POST removed). Tests: gate probe arms with a world / not without; planned_write_set
  derived from the plan; **counter unchanged across the gate-probe compile** (the injection
  call-index fence). 5 tests.
- **C2** (aacb92b) build the guaranteed pre-completion sweep (`cadence/sweep.py`). Fires once
  after the last worker drains; re-observes every still-owed surface (incl. the never-touched
  §4 shadow); unreachable → uncovered valve; fresh surfaces not re-swept. 3 tests.
- **C3** (2dd5e20) route GATE_SHADOW probes through `evaluate_gate_probe` (`enforcing == True`)
  in `run_v2_detection`, before the status fast path. Trapped gate → interrupt; healthy →
  clean; no auth context → disqualified. 3 tests.
- **C4** (f9febab) write-surface footprint policy (`write_surface.py`). Attachment gains a
  WRITE_FOOTPRINT disposition (replaces silent PASSIVE); off-footprint → DRIFT; in-footprint →
  verify-or-UNCOVERED_CAUTION; the surface never enters the drift+persistence path. 6 tests +
  3 updated.
- **C5 wiring fixes** (488eb44, found while validating): PRE_COMPLETION probes excluded from
  barriers (the gate fired at a barrier via fallback-to-all-targets, routing through a flaky
  replan that aborted on `orchestrator_invalid`) → the gate now detects at the sweep, which
  emits the M6 interrupt marker directly (no replan); the `uncovered` trace event registered;
  the sweep client kept open through write-footprint eval; M6 attributes a gate interrupt on
  the §4 shadow to the `/repo/validate` injection (instrument surface map) → FIR 0, not 1.

Suite grew 373 → 390 (both flag states green at every checkpoint); byte-identity 27/27 both
states throughout (banked f5b44b6 restored). No production code touched flag-off behavior.

## C5 full seen sweep (real V2Conductor; SEEN only; matrix NOT run)

| cell | detected | probe that caught it | int / replan | FIR | baseline<inj | cost |
|---|---|---|---|---|---|---|
| a1+endpoint_404 | YES | status_fast_path | 1 / 0 | 0.0 | — | $0.54 |
| a1+schema_drift | YES | schema_shape (arm-time baseline) | 1 / 0 | 0.0 | YES (1<12) | $0.35 |
| a1+token_expiry | YES | status_fast_path (multi-surface) | 2 / 2 | 0.0 | — | $0.68 |
| c1+doc_contradiction | YES¹ | value_changed (arm-time baseline) | 1 / 0 | 0.0 | YES (1<6) | $0.46 |
| **b1+gate_skip_trap** | **YES** | **gate_shadow (§4 premise, pre-completion sweep)** | 1 / 0 | **0.0** | — | $0.30 |
| a1+clean | QUIET | — | 0 / 0 | — | — | $0.26 |
| c1+clean | QUIET | — | 0 / 0 | — | — | $0.37 |
| **b1+clean** | **QUIET** | — (settings.yaml write → UNCOVERED_CAUTION) | 0 / 0 | — | — | $0.40 |

All five seen categories detect for the right reason (content via the arm-time baseline
captured before injection; the gate via the armed §4 probe fired by the pre-completion sweep
and evaluated by evaluate_gate_probe); every clean cell is QUIET, including b1+clean (was
FIR 1.0). FIR 0.0 on every injected cell.

¹ **doc_contradiction**: the FIRST attempt missed — the LLM compile grounded the passage
assumption to a template representative (`/docs/passages/ops-shipping`) instead of the injected
`/docs/passages/pol-returns`, so the probe watched the wrong passage. The re-run grounded to
pol-returns and detected (value_changed, arm_time@1 < injection@6). This is **pre-existing
compile-grounding non-determinism** for passage-SPECIFIC content injections (schema_drift is
immune — its injection is surface-wide, so the representative SKU still drifts) — NOT a D31
regression (D31 did not touch the D30 content path). Named as a residual below.

## Additional named residual (beyond the D31 deviation)

- **Compile passage-grounding non-determinism:** a content injection that targets ONE specific
  instance of a templated surface (doc_contradiction → a single passage) is detected only when
  the compile grounds the assumption to THAT instance; the template-instantiation grounding
  picks the lexicographically-first representative, which may differ from the injected instance.
  A category-blind fix (extract every plan-named instance, or arm the named instance concretely)
  belongs to the D4 compile path, not D31; recorded as a threat-to-validity, not patched.

## Fences (dominant)

Side-channel world handle (counter-neutral §4 read; assert injection call-index unchanged);
frozen D28 status fast path untouched; D28 persistence for non-write surfaces unchanged
(write-surface epoch check is additive); category-blind + injection-blind; no escrow/held-out
read; matrix NOT fired; $0 dollars (count submetric only); byte-identity flag-off preserved
(banked f5b44b6 restored); D29/D30 preserved; no push.
