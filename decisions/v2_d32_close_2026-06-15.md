# D32 close-out — family-template grounding under-coverage fix

**Date:** 2026-06-15. **Flag:** behind `TRIPWIRE_V2`; flag-off byte-identical to Phase 1.
**Matrix:** NOT run. **Held-out:** not read, not run. Ruling: `deviations.md` D32; build record:
`decisions/v2_d32_grounding_undercoverage_2026-06-15.md`.

## What changed (C1)

`sentinel_v2/compile_probes.py` only. `_instantiate` now returns the FULL sorted family
(was `sorted(glob)[0]`); `ground_surface` returns a `SurfaceGrounding`; a `{param}` family
template grounds to (1) the plan-named concrete ids the soft set names directly inside the
family glob, else (2) ALL bounded members within `FAMILY_MEMBER_CAP` (=24), else (3)
UNCOVERED_CAUTION; budget overflow → UNCOVERED_CAUTION. `compile_pipeline` routes EACH armed
member through the EXISTING per-surface policy (D31 write-footprint / gate eval / D30
baseline+drift) — composes, never bypasses. Invented-concrete-id (D5/D8, single representative)
and concrete hallucination (N2, loud) unchanged. LLM compile prompt byte-unchanged.

## C2(a) — full seen sweep through the real V2Conductor (regression check)

`analysis/v2_d31_revalidate` (real path), `runs/v2_d31_revalidate/summary.json`. ALL five seen
categories DETECT for the right reason; ALL three clean cells QUIET — identical to the D31 C5
baseline, so **no regression** from the grounding change. FIR 0.0 throughout; $0 dollars
(detection is deterministic side-channel). Dev-run spend $3.85.

| cell | detected | probe | int/replan | FIR | right reason | cost |
|---|---|---|---|---|---|---|
| a1+endpoint_404 | True | status_fast_path | 1/1 | 0.0 | — | $0.656 |
| a1+schema_drift | True | schema_shape | 1/1 | 0.0 | base@<12 (arm@1) | $0.525 |
| a1+token_expiry | True | status_fast_path | 2/2 | 0.0 | — | $0.682 |
| c1+doc_contradiction | True | value_changed | 1/1 | 0.0 | base@1 < inj@6 | $0.695 |
| b1+gate_skip_trap | True | gate_shadow | 1/0 | 0.0 | — | $0.285 |
| a1+clean | False (quiet) | — | 0/0 | — | — | $0.316 |
| c1+clean | False (quiet) | — | 0/0 | — | — | $0.358 |
| b1+clean | False (quiet) | — | 0/0 | — | — | $0.329 |

b1+clean stays quiet with `write_footprints=['/repo/files/config/settings.yaml']` and the §4
gate armed — the D31 footprint policy holds under D32 (no false positive on the legit write).

## C2(b) — doc_contradiction under BOTH plans (the decisive test)

**Concrete-naming c1 plan** (C2a sweep cell): detects, no regression. The model names
pol-returns concretely (value /content probe); under D32 the family template grounds to the
plan-named {pol-returns} — coverage unchanged.

**Generic-naming variant** (decisive; `analysis/v2_d32_generic.py controlled`,
`runs/v2_d32_generic/summary_controlled.json`). The compile output is controlled to the
canonical generic-naming soft set — ONE family-level assumption `/docs/passages/{passage_id}`
with a `/content` dependency and NO concrete passage id — removing LLM-compile variance, exactly
as the diagnosis controlled the plan. The arm-time baseline, barriers, pre-completion sweep,
corroboration, and replan are the real path.

| cell | detected | armed family | pol-returns lens | probe | base<inj | int/replan | cost |
|---|---|---|---|---|---|---|---|
| generic doc_contradiction | **True** | 6/6 (full) | field_read /content value_changed | value_changed | arm@1 < inj@6 | 1/1 | $0.134 |
| generic clean | False (quiet) | 6/6 (full) | field_read /content value_changed | — | n/a | 0/0 | $0.081 |

**The decisive A/B (deterministic):** the OLD `sorted(glob)[0]` arms only
`/docs/passages/ops-shipping` → pol-returns NOT covered → **MISS**. D32 arms the full family
(6) → pol-returns covered as a family member → **DETECT** (right reason: clean arm-time
baseline@1 < injection@6; caught by the value_changed probe on pol-returns). Clean stays quiet
(read-only passages don't change). This is the test the diagnosis flagged and did not spend.

**Honest supporting note (real-LLM generic plan; `summary_real_llm_plan.json`).** Run with a
real (uncontrolled) generic plan, D32 still ARMED pol-returns (`pol_returns_armed=True`),
confirming the grounding fix end-to-end. It did NOT detect because the real LLM, under that
plan, reconstructed concrete passages from the c1 goal/appendix and assigned a STRUCTURE
(schema) lens — not a `/content` value lens — so the content-only swap was invisible. That is a
LENS/EXTRACTION property (which lens the model emits), orthogonal to D32 grounding (which
surfaces are armed), and is named as a residual below. The concrete c1 cell detects precisely
because its plan elicits the `/content` value lens.

## Budget / count submetric

Dollars: **$0** — grounding is deterministic side-channel; KG3 (total_cost_usd) unaffected. The
detection-machinery dollar impact is $0. Count submetric: a family template now arms N members
instead of 1. Deterministic generic A/B (the decisive case): D32 arms the **full 6-member**
passage family (6 arm-probes) vs the old code's **1** (`sorted(glob)[0]`=ops-shipping) → **+5**
arm-probes, $0 dollars. In the concrete seen cells the template grounds to the plan-named id
(c1+doc_contradiction armed 8 surfaces and still detects via value_changed), so the delta is
neutral-to-fewer there (D32 drops the spurious lexicographic-first member the old code armed).
Dev-run spend: seen sweep $3.85 + controlled C2(b) $0.216 + real-LLM generic $0.720 = **$4.78**
(within the ~$6 budget).

## Named residual

**Lens/extraction under a generic plan.** D32 guarantees the injected member's SURFACE is armed
as a family member; detecting a content-only swap additionally requires a content/value lens on
that member. A generic plan that does not name a content-field dependency yields structure
probes, which do not see a content swap. This is an extraction (lens-selection) concern, not a
grounding one; out of D32 scope.

## Close assertions

- Full suite both flag states: **399 passed** (flag off) / **399 passed** (flag on); suite grew
  390→399 (nine D32 tests). Byte-identity **27/27** both (`analysis.replay_check`).
- LLM compile prompt byte-unchanged (sha256 of `prompts/v2_compile.md` +
  `prompts/v2_compile_fewshot.json` unchanged); D28/D29/D30/D31 module paths untouched
  (`git diff` empty for `corroboration.py`, `cadence/`, `run_v2_loop.py`, `attachment.py`,
  `gate_route.py`, `arms.py`, `write_surface.py`, `world/server.py`).
- $0 dollar impact; count submetric delta reported above (+5 in the generic family case).
- No held-out read or run; the one-shot matrix NOT run; not pushed.
- Commit chain: C0 `4f310ef` → C1 `58a4714` → C2 `37a8424` → CLOSE.
